# mantle_widgets example

A filled `Button` under `BasicTheme`. Widgets get their look from a theme
delegate — this is the usual app pairing.

```bash
cd packages/mantle_widgets/example
flutter run -d chrome
```
