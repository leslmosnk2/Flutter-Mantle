# ActionIconGroup

Mantine `ActionIcon.Group`.

Inspired by Mantine [`ActionIconGroup`](https://mantine.dev/core/action-icon-group/).

## Constructors

- `ActionIconGroup(...)` (unnamed / `default`)

## Properties

| Prop | Type | Default |
|---|---|---|
| `children` | `List<Widget>` | required |
| `orientation` | `Axis?` | `Axis.horizontal` |
| `borderWidth` | `double?` | `1` |

## Example

```dart
ActionIconGroup(
  children: const [],
)
```

## Slots

Theme delegates implement:

- `root`

See [`mantle_basic` ActionIconGroup](/basic/buttons/ActionIconGroup/) for the baseline paint, and [architecture](/widgets/architecture) for how lookup works.
