# Collapse

Baseline `CollapseDelegate` implementation (`BasicCollapseDelegate`).

Baseline `CollapseDelegate`: animated height (and optional opacity).

Source: `lib/src/widgets/miscellaneous/collapse.dart`.

`BasicTheme` registers `CollapseDelegate: BasicCollapseDelegate()`.

Public constructors and props: [Collapse](/widgets/miscellaneous/Collapse/).
