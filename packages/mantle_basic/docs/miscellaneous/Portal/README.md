# Portal

Baseline `PortalDelegate` implementation (`BasicPortalDelegate`).

Baseline `PortalDelegate`: paints [PortalContext.child] in an `Overlay`.

Source: `lib/src/widgets/miscellaneous/portal.dart`.

`BasicTheme` registers `PortalDelegate: BasicPortalDelegate()`.

Public constructors and props: [Portal](/widgets/miscellaneous/Portal/).
