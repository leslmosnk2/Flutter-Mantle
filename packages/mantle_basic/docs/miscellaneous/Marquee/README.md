# Marquee

Baseline `MarqueeDelegate` implementation (`BasicMarqueeDelegate`).

Baseline `MarqueeDelegate`: a looping translating strip.

Source: `lib/src/widgets/miscellaneous/marquee.dart`.

`BasicTheme` registers `MarqueeDelegate: BasicMarqueeDelegate()`.

Public constructors and props: [Marquee](/widgets/miscellaneous/Marquee/).
