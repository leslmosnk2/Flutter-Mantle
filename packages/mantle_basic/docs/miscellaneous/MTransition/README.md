# MTransition

Baseline `MTransitionDelegate` implementation (`BasicMTransitionDelegate`).

Baseline `MTransitionDelegate`: fade / scale / slide around the child.

Source: `lib/src/widgets/miscellaneous/transition.dart`.

`BasicTheme` registers `MTransitionDelegate: BasicMTransitionDelegate()`.

Public constructors and props: [MTransition](/widgets/miscellaneous/MTransition/).
