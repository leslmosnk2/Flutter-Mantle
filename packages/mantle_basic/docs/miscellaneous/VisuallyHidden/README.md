# VisuallyHidden

Baseline `VisuallyHiddenDelegate` implementation (`BasicVisuallyHiddenDelegate`).

Baseline `VisuallyHiddenDelegate`: 1×1 clip that keeps semantics.

Source: `lib/src/widgets/miscellaneous/visually_hidden.dart`.

`BasicTheme` registers `VisuallyHiddenDelegate: BasicVisuallyHiddenDelegate()`.

Public constructors and props: [VisuallyHidden](/widgets/miscellaneous/VisuallyHidden/).
