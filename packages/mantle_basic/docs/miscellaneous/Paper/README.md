# Paper

Baseline `PaperDelegate` implementation (`BasicPaperDelegate`).

Baseline `PaperDelegate`: body surface with optional border and shadow.

Source: `lib/src/widgets/miscellaneous/paper.dart`.

`BasicTheme` registers `PaperDelegate: BasicPaperDelegate()`.

Public constructors and props: [Paper](/widgets/miscellaneous/Paper/).
