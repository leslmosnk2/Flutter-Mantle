# FocusTrap

Baseline `FocusTrapDelegate` implementation (`BasicFocusTrapDelegate`).

Baseline `FocusTrapDelegate`: a `FocusScope` that autofocuses when active.

Source: `lib/src/widgets/miscellaneous/focus_trap.dart`.

`BasicTheme` registers `FocusTrapDelegate: BasicFocusTrapDelegate()`.

Public constructors and props: [FocusTrap](/widgets/miscellaneous/FocusTrap/).
