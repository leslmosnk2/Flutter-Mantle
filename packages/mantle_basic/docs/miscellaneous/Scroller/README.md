# Scroller

Baseline `ScrollerDelegate` implementation (`BasicScrollerDelegate`).

Baseline `ScrollerDelegate`: horizontal strip with chevron controls.

Source: `lib/src/widgets/miscellaneous/scroller.dart`.

`BasicTheme` registers `ScrollerDelegate: BasicScrollerDelegate()`.

Public constructors and props: [Scroller](/widgets/miscellaneous/Scroller/).
