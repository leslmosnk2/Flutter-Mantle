# Divider

Baseline `DividerDelegate` implementation (`BasicDividerDelegate`).

Baseline `DividerDelegate`: a hairline rule with an optional label.

Source: `lib/src/widgets/miscellaneous/divider.dart`.

`BasicTheme` registers `DividerDelegate: BasicDividerDelegate()`.

Public constructors and props: [Divider](/widgets/miscellaneous/Divider/).
