# Box

Baseline `BoxDelegate` implementation (`BasicBoxDelegate`).

Baseline `BoxDelegate`: padding, margin, fill, radius, and size.

Source: `lib/src/widgets/miscellaneous/box.dart`.

`BasicTheme` registers `BoxDelegate: BasicBoxDelegate()`.

Public constructors and props: [Box](/widgets/miscellaneous/Box/).
