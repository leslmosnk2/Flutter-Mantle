# ScrollArea

Baseline `ScrollAreaDelegate` implementation (`BasicScrollAreaDelegate`).

Baseline `ScrollAreaDelegate`: nested scroll views plus `RawScrollbar`.

Source: `lib/src/widgets/miscellaneous/scroll_area.dart`.

`BasicTheme` registers `ScrollAreaDelegate: BasicScrollAreaDelegate()`.

Public constructors and props: [ScrollArea](/widgets/miscellaneous/ScrollArea/).
