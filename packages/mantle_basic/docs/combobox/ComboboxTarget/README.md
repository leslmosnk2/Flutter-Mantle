# ComboboxTarget

Baseline `ComboboxTargetDelegate` implementation (`BasicComboboxTargetDelegate`).

Baseline `ComboboxTargetDelegate`.

Source: `lib/src/widgets/combobox/combobox.dart`.

`BasicTheme` registers `ComboboxTargetDelegate: BasicComboboxTargetDelegate()`.

Public constructors and props: [ComboboxTarget](/widgets/combobox/ComboboxTarget/).
