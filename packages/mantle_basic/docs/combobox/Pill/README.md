# Pill

Baseline `PillDelegate` implementation (`BasicPillDelegate`).

Baseline `PillDelegate`.

Source: `lib/src/widgets/combobox/pill.dart`.

`BasicTheme` registers `PillDelegate: BasicPillDelegate()`.

Public constructors and props: [Pill](/widgets/combobox/Pill/).
