# ComboboxOption

Baseline `ComboboxOptionDelegate` implementation (`BasicComboboxOptionDelegate`).

Baseline `ComboboxOptionDelegate`.

Source: `lib/src/widgets/combobox/combobox.dart`.

`BasicTheme` registers `ComboboxOptionDelegate: BasicComboboxOptionDelegate()`.

Public constructors and props: [ComboboxOption](/widgets/combobox/ComboboxOption/).
