# Cascader

Baseline `CascaderDelegate` implementation (`BasicCascaderDelegate`).

Baseline `CascaderDelegate`: path selection through columns or a flat list.

Source: `lib/src/widgets/combobox/cascader.dart`.

`BasicTheme` registers `CascaderDelegate: BasicCascaderDelegate()`.

Public constructors and props: [Cascader](/widgets/combobox/Cascader/).
