# Combobox

Baseline `ComboboxDelegate` implementation (`BasicComboboxDelegate`).

Baseline `ComboboxDelegate`: target + dropdown with `ComboboxScope`.

Source: `lib/src/widgets/combobox/combobox.dart`.

`BasicTheme` registers `ComboboxDelegate: BasicComboboxDelegate()`.

Public constructors and props: [Combobox](/widgets/combobox/Combobox/).
