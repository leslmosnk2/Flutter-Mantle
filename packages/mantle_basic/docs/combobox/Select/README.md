# Select

Baseline `SelectDelegate` implementation (`BasicSelectDelegate`).

Baseline `SelectDelegate`: input + Combobox options.

Source: `lib/src/widgets/combobox/select.dart`.

`BasicTheme` registers `SelectDelegate: BasicSelectDelegate()`.

Public constructors and props: [Select](/widgets/combobox/Select/).
