# PopoverDropdown

Baseline `PopoverDropdownDelegate` implementation (`BasicPopoverDropdownDelegate`).

Baseline `PopoverDropdownDelegate`: the floating panel.

Source: `lib/src/widgets/overlays/popover.dart`.

`BasicTheme` registers `PopoverDropdownDelegate: BasicPopoverDropdownDelegate()`.

Public constructors and props: [PopoverDropdown](/widgets/overlays/PopoverDropdown/).
