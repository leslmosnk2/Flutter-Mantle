# ModalRoot

Baseline `ModalRootDelegate` implementation (`BasicModalRootDelegate`).

Baseline ModalRootDelegate: barrier + child without header chrome.

Source: `lib/src/widgets/overlays/modal.dart`.

`BasicTheme` registers `ModalRootDelegate: BasicModalRootDelegate()`.

Public constructors and props: [ModalRoot](/widgets/overlays/ModalRoot/).
