# MenuTarget

Baseline `MenuTargetDelegate` implementation (`BasicMenuTargetDelegate`).

Baseline `MenuTargetDelegate`: the menu trigger.

Source: `lib/src/widgets/overlays/menu.dart`.

`BasicTheme` registers `MenuTargetDelegate: BasicMenuTargetDelegate()`.

Public constructors and props: [MenuTarget](/widgets/overlays/MenuTarget/).
