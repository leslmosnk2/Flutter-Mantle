# HoverCardTarget

Baseline `HoverCardTargetDelegate` implementation (`BasicHoverCardTargetDelegate`).

Baseline `HoverCardTargetDelegate`: the hover target.

Source: `lib/src/widgets/overlays/hover_card.dart`.

`BasicTheme` registers `HoverCardTargetDelegate: BasicHoverCardTargetDelegate()`.

Public constructors and props: [HoverCardTarget](/widgets/overlays/HoverCardTarget/).
