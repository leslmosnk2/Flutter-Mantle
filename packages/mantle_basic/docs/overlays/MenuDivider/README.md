# MenuDivider

Baseline `MenuDividerDelegate` implementation (`BasicMenuDividerDelegate`).

Baseline `MenuDividerDelegate`: a horizontal rule.

Source: `lib/src/widgets/overlays/menu.dart`.

`BasicTheme` registers `MenuDividerDelegate: BasicMenuDividerDelegate()`.

Public constructors and props: [MenuDivider](/widgets/overlays/MenuDivider/).
