# MenubarTarget

Baseline `MenubarTargetDelegate` implementation (`BasicMenubarTargetDelegate`).

Baseline `MenubarTargetDelegate`: a top-level trigger button.

Source: `lib/src/widgets/overlays/menubar.dart`.

`BasicTheme` registers `MenubarTargetDelegate: BasicMenubarTargetDelegate()`.

Public constructors and props: [MenubarTarget](/widgets/overlays/MenubarTarget/).
