# PopoverTarget

Baseline `PopoverTargetDelegate` implementation (`BasicPopoverTargetDelegate`).

Baseline `PopoverTargetDelegate`: the popover anchor.

Source: `lib/src/widgets/overlays/popover.dart`.

`BasicTheme` registers `PopoverTargetDelegate: BasicPopoverTargetDelegate()`.

Public constructors and props: [PopoverTarget](/widgets/overlays/PopoverTarget/).
