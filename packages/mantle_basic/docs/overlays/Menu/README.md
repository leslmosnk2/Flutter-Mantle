# Menu

Baseline `MenuDelegate` implementation (`BasicMenuDelegate`).

Baseline MenuDelegate: target plus a list of items when opened.

Source: `lib/src/widgets/overlays/menu.dart`.

`BasicTheme` registers `MenuDelegate: BasicMenuDelegate()`.

Public constructors and props: [Menu](/widgets/overlays/Menu/).
