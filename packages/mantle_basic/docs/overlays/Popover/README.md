# Popover

Baseline `PopoverDelegate` implementation (`BasicPopoverDelegate`).

Baseline PopoverDelegate: target plus a dropdown when opened.

Source: `lib/src/widgets/overlays/popover.dart`.

`BasicTheme` registers `PopoverDelegate: BasicPopoverDelegate()`.

Public constructors and props: [Popover](/widgets/overlays/Popover/).
