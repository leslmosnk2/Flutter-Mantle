# HoverCard

Baseline `HoverCardDelegate` implementation (`BasicHoverCardDelegate`).

Baseline HoverCardDelegate: popover that opens while the pointer is over it.

Source: `lib/src/widgets/overlays/hover_card.dart`.

`BasicTheme` registers `HoverCardDelegate: BasicHoverCardDelegate()`.

Public constructors and props: [HoverCard](/widgets/overlays/HoverCard/).
