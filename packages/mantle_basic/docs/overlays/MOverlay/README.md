# MOverlay

Baseline `MOverlayDelegate` implementation (`BasicMOverlayDelegate`).

Baseline `MOverlayDelegate`: a dimming barrier over the parent.

Source: `lib/src/widgets/overlays/overlay.dart`.

`BasicTheme` registers `MOverlayDelegate: BasicMOverlayDelegate()`.

Public constructors and props: [MOverlay](/widgets/overlays/MOverlay/).
