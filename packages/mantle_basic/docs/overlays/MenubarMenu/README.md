# MenubarMenu

Baseline `MenubarMenuDelegate` implementation (`BasicMenubarMenuDelegate`).

Baseline `MenubarMenuDelegate`: one top-level menu in the bar.

Source: `lib/src/widgets/overlays/menubar.dart`.

`BasicTheme` registers `MenubarMenuDelegate: BasicMenubarMenuDelegate()`.

Public constructors and props: [MenubarMenu](/widgets/overlays/MenubarMenu/).
