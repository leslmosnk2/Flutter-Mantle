# HoverCardDropdown

Baseline `HoverCardDropdownDelegate` implementation (`BasicHoverCardDropdownDelegate`).

Baseline `HoverCardDropdownDelegate`: the hover dropdown panel.

Source: `lib/src/widgets/overlays/hover_card.dart`.

`BasicTheme` registers `HoverCardDropdownDelegate: BasicHoverCardDropdownDelegate()`.

Public constructors and props: [HoverCardDropdown](/widgets/overlays/HoverCardDropdown/).
