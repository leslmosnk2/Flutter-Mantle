# Modal

Baseline `ModalDelegate` implementation (`BasicModalDelegate`).

Baseline `ModalDelegate`: centered panel over a barrier.

Source: `lib/src/widgets/overlays/modal.dart`.

`BasicTheme` registers `ModalDelegate: BasicModalDelegate()`.

Public constructors and props: [Modal](/widgets/overlays/Modal/).
