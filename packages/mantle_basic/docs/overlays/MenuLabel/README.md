# MenuLabel

Baseline `MenuLabelDelegate` implementation (`BasicMenuLabelDelegate`).

Baseline `MenuLabelDelegate`: a non-interactive section heading.

Source: `lib/src/widgets/overlays/menu.dart`.

`BasicTheme` registers `MenuLabelDelegate: BasicMenuLabelDelegate()`.

Public constructors and props: [MenuLabel](/widgets/overlays/MenuLabel/).
