# Tooltip

Baseline `TooltipDelegate` implementation (`BasicTooltipDelegate`).

Baseline TooltipDelegate: shows `label` on hover or long-press.

Source: `lib/src/widgets/overlays/tooltip.dart`.

`BasicTheme` registers `TooltipDelegate: BasicTooltipDelegate()`.

Public constructors and props: [Tooltip](/widgets/overlays/Tooltip/).
