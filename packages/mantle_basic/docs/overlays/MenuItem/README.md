# MenuItem

Baseline `MenuItemDelegate` implementation (`BasicMenuItemDelegate`).

Baseline `MenuItemDelegate`: a tappable row.

Source: `lib/src/widgets/overlays/menu.dart`.

`BasicTheme` registers `MenuItemDelegate: BasicMenuItemDelegate()`.

Public constructors and props: [MenuItem](/widgets/overlays/MenuItem/).
