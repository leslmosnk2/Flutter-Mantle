# Menubar

Baseline `MenubarDelegate` implementation (`BasicMenubarDelegate`).

Baseline `MenubarDelegate`: a horizontal row of `MenubarMenu` widgets.

Source: `lib/src/widgets/overlays/menubar.dart`.

`BasicTheme` registers `MenubarDelegate: BasicMenubarDelegate()`.

Public constructors and props: [Menubar](/widgets/overlays/Menubar/).
