# MenuDropdown

Baseline `MenuDropdownDelegate` implementation (`BasicMenuDropdownDelegate`).

Baseline `MenuDropdownDelegate`: the item list panel.

Source: `lib/src/widgets/overlays/menu.dart`.

`BasicTheme` registers `MenuDropdownDelegate: BasicMenuDropdownDelegate()`.

Public constructors and props: [MenuDropdown](/widgets/overlays/MenuDropdown/).
