# LoadingOverlay

Baseline `LoadingOverlayDelegate` implementation (`BasicLoadingOverlayDelegate`).

Baseline LoadingOverlayDelegate: stacks a barrier and loader over `child`.

Source: `lib/src/widgets/overlays/loading_overlay.dart`.

`BasicTheme` registers `LoadingOverlayDelegate: BasicLoadingOverlayDelegate()`.

Public constructors and props: [LoadingOverlay](/widgets/overlays/LoadingOverlay/).
