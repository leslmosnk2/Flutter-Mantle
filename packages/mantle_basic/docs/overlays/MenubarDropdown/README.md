# MenubarDropdown

Baseline `MenubarDropdownDelegate` implementation (`BasicMenubarDropdownDelegate`).

Baseline `MenubarDropdownDelegate`: dropdown under a `MenubarTarget`.

Source: `lib/src/widgets/overlays/menubar.dart`.

`BasicTheme` registers `MenubarDropdownDelegate: BasicMenubarDropdownDelegate()`.

Public constructors and props: [MenubarDropdown](/widgets/overlays/MenubarDropdown/).
