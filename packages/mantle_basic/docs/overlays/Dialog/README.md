# Dialog

Baseline `DialogDelegate` implementation (`BasicDialogDelegate`).

Baseline `DialogDelegate`: a small corner panel without a barrier.

Source: `lib/src/widgets/overlays/dialog.dart`.

`BasicTheme` registers `DialogDelegate: BasicDialogDelegate()`.

Public constructors and props: [Dialog](/widgets/overlays/Dialog/).
