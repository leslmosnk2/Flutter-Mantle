# Drawer

Baseline `DrawerDelegate` implementation (`BasicDrawerDelegate`).

Baseline `DrawerDelegate`: a sliding side panel over a barrier.

Source: `lib/src/widgets/overlays/drawer.dart`.

`BasicTheme` registers `DrawerDelegate: BasicDrawerDelegate()`.

Public constructors and props: [Drawer](/widgets/overlays/Drawer/).
