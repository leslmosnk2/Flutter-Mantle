# Affix

Baseline `AffixDelegate` implementation (`BasicAffixDelegate`).

Baseline `AffixDelegate`: `Stack` + `Positioned` in the parent.

Source: `lib/src/widgets/overlays/affix.dart`.

`BasicTheme` registers `AffixDelegate: BasicAffixDelegate()`.

Public constructors and props: [Affix](/widgets/overlays/Affix/).
