# Progress

Baseline `ProgressDelegate` implementation (`BasicProgressDelegate`).

Baseline `ProgressDelegate`: a single horizontal bar.

Source: `lib/src/widgets/feedback/progress.dart`.

`BasicTheme` registers `ProgressDelegate: BasicProgressDelegate()`.

Public constructors and props: [Progress](/widgets/feedback/Progress/).
