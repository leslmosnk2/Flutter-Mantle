# RingProgress

Baseline `RingProgressDelegate` implementation (`BasicRingProgressDelegate`).

Baseline `RingProgressDelegate`: circular sections and a hole label.

Source: `lib/src/widgets/feedback/ring_progress.dart`.

`BasicTheme` registers `RingProgressDelegate: BasicRingProgressDelegate()`.

Public constructors and props: [RingProgress](/widgets/feedback/RingProgress/).
