# Skeleton

Baseline `SkeletonDelegate` implementation (`BasicSkeletonDelegate`).

Baseline `SkeletonDelegate`: pulsing placeholder or overlay.

Source: `lib/src/widgets/feedback/skeleton.dart`.

`BasicTheme` registers `SkeletonDelegate: BasicSkeletonDelegate()`.

Public constructors and props: [Skeleton](/widgets/feedback/Skeleton/).
