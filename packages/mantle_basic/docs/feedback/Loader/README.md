# Loader

Baseline `LoaderDelegate` implementation (`BasicLoaderDelegate`).

Baseline `LoaderDelegate`: oval, bars, or dots spinner.

Source: `lib/src/widgets/feedback/loader.dart`.

`BasicTheme` registers `LoaderDelegate: BasicLoaderDelegate()`.

Public constructors and props: [Loader](/widgets/feedback/Loader/).
