# SemiCircleProgress

Baseline `SemiCircleProgressDelegate` implementation (`BasicSemiCircleProgressDelegate`).

Baseline `SemiCircleProgressDelegate`: a half-ring with a label.

Source: `lib/src/widgets/feedback/semi_circle_progress.dart`.

`BasicTheme` registers `SemiCircleProgressDelegate: BasicSemiCircleProgressDelegate()`.

Public constructors and props: [SemiCircleProgress](/widgets/feedback/SemiCircleProgress/).
