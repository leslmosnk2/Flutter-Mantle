# Alert

Baseline `AlertDelegate` implementation (`BasicAlertDelegate`).

Baseline `AlertDelegate`: variant-colored chrome with icon, copy, and close.

Source: `lib/src/widgets/feedback/alert.dart`.

`BasicTheme` registers `AlertDelegate: BasicAlertDelegate()`.

Public constructors and props: [Alert](/widgets/feedback/Alert/).
