# TabsTab

Baseline `TabsTabDelegate` implementation (`BasicTabsTabDelegate`).

Baseline `TabsTabDelegate`: a tab control with an active indicator.

Source: `lib/src/widgets/navigation/tabs.dart`.

`BasicTheme` registers `TabsTabDelegate: BasicTabsTabDelegate()`.

Public constructors and props: [TabsTab](/widgets/navigation/TabsTab/).
