# Pagination

Baseline `PaginationDelegate` implementation (`BasicPaginationDelegate`).

Baseline `PaginationDelegate`: a controlled page-number row.

Source: `lib/src/widgets/navigation/pagination.dart`.

`BasicTheme` registers `PaginationDelegate: BasicPaginationDelegate()`.

Public constructors and props: [Pagination](/widgets/navigation/Pagination/).
