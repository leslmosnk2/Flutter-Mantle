# TabsPanel

Baseline `TabsPanelDelegate` implementation (`BasicTabsPanelDelegate`).

Baseline `TabsPanelDelegate`: shows content when the tab is active.

Source: `lib/src/widgets/navigation/tabs.dart`.

`BasicTheme` registers `TabsPanelDelegate: BasicTabsPanelDelegate()`.

Public constructors and props: [TabsPanel](/widgets/navigation/TabsPanel/).
