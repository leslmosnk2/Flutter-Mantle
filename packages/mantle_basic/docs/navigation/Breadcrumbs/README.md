# Breadcrumbs

Baseline `BreadcrumbsDelegate` implementation (`BasicBreadcrumbsDelegate`).

Baseline `BreadcrumbsDelegate`: a wrapping trail with separators.

Source: `lib/src/widgets/navigation/breadcrumbs.dart`.

`BasicTheme` registers `BreadcrumbsDelegate: BasicBreadcrumbsDelegate()`.

Public constructors and props: [Breadcrumbs](/widgets/navigation/Breadcrumbs/).
