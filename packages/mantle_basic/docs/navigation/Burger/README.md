# Burger

Baseline `BurgerDelegate` implementation (`BasicBurgerDelegate`).

Baseline `BurgerDelegate`: a three-line menu toggle.

Source: `lib/src/widgets/navigation/burger.dart`.

`BasicTheme` registers `BurgerDelegate: BasicBurgerDelegate()`.

Public constructors and props: [Burger](/widgets/navigation/Burger/).
