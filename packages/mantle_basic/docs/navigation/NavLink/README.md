# NavLink

Baseline `NavLinkDelegate` implementation (`BasicNavLinkDelegate`).

Baseline `NavLinkDelegate`: a full-width navigation row.

Source: `lib/src/widgets/navigation/nav_link.dart`.

`BasicTheme` registers `NavLinkDelegate: BasicNavLinkDelegate()`.

Public constructors and props: [NavLink](/widgets/navigation/NavLink/).
