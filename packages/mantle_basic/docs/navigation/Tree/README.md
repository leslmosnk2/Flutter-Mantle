# Tree

Baseline `TreeDelegate` implementation (`BasicTreeDelegate`).

Baseline `TreeDelegate`: maps `TreeNodeData` to recursive `TreeNode`s.

Source: `lib/src/widgets/navigation/tree.dart`.

`BasicTheme` registers `TreeDelegate: BasicTreeDelegate()`.

Public constructors and props: [Tree](/widgets/navigation/Tree/).
