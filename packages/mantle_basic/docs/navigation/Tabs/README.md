# Tabs

Baseline `TabsDelegate` implementation (`BasicTabsDelegate`).

Baseline `TabsDelegate`: publishes `TabsScope` and lays out children.

Source: `lib/src/widgets/navigation/tabs.dart`.

`BasicTheme` registers `TabsDelegate: BasicTabsDelegate()`.

Public constructors and props: [Tabs](/widgets/navigation/Tabs/).
