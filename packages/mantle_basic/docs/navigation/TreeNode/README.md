# TreeNode

Baseline `TreeNodeDelegate` implementation (`BasicTreeNodeDelegate`).

Baseline `TreeNodeDelegate`: a tappable label with nested children.

Source: `lib/src/widgets/navigation/tree.dart`.

`BasicTheme` registers `TreeNodeDelegate: BasicTreeNodeDelegate()`.

Public constructors and props: [TreeNode](/widgets/navigation/TreeNode/).
