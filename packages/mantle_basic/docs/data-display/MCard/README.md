# MCard

Baseline `MCardDelegate` implementation (`BasicMCardDelegate`).

Baseline `MCardDelegate`: paper column.

Source: `lib/src/widgets/data_display/card.dart`.

`BasicTheme` registers `MCardDelegate: BasicMCardDelegate()`.

Public constructors and props: [MCard](/widgets/data-display/MCard/).
