# RollingNumber

Baseline `RollingNumberDelegate` implementation (`BasicRollingNumberDelegate`).

Baseline `RollingNumberDelegate`: formatted text with a cross-fade.

Source: `lib/src/widgets/data_display/rolling_number.dart`.

`BasicTheme` registers `RollingNumberDelegate: BasicRollingNumberDelegate()`.

Public constructors and props: [RollingNumber](/widgets/data-display/RollingNumber/).
