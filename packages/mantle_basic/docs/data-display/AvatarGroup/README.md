# AvatarGroup

Baseline `AvatarGroupDelegate` implementation (`BasicAvatarGroupDelegate`).

Baseline `AvatarGroupDelegate`: overlapping row.

Source: `lib/src/widgets/data_display/avatar.dart`.

`BasicTheme` registers `AvatarGroupDelegate: BasicAvatarGroupDelegate()`.

Public constructors and props: [AvatarGroup](/widgets/data-display/AvatarGroup/).
