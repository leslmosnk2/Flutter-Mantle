# Spoiler

Baseline `SpoilerDelegate` implementation (`BasicSpoilerDelegate`).

Baseline `SpoilerDelegate`: clip + show more/less.

Source: `lib/src/widgets/data_display/spoiler.dart`.

`BasicTheme` registers `SpoilerDelegate: BasicSpoilerDelegate()`.

Public constructors and props: [Spoiler](/widgets/data-display/Spoiler/).
