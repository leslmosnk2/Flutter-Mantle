# NumberFormatter

Baseline `NumberFormatterDelegate` implementation (`BasicNumberFormatterDelegate`).

Baseline `NumberFormatterDelegate`: formatted `Text`.

Source: `lib/src/widgets/data_display/number_formatter.dart`.

`BasicTheme` registers `NumberFormatterDelegate: BasicNumberFormatterDelegate()`.

Public constructors and props: [NumberFormatter](/widgets/data-display/NumberFormatter/).
