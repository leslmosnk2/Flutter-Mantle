# Timeline

Baseline `TimelineDelegate` implementation (`BasicTimelineDelegate`).

Baseline `TimelineDelegate`: vertical list with `TimelineScope`.

Source: `lib/src/widgets/data_display/timeline.dart`.

`BasicTheme` registers `TimelineDelegate: BasicTimelineDelegate()`.

Public constructors and props: [Timeline](/widgets/data-display/Timeline/).
