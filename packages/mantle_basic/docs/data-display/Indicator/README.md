# Indicator

Baseline `IndicatorDelegate` implementation (`BasicIndicatorDelegate`).

Baseline `IndicatorDelegate`: overlay marker on a child.

Source: `lib/src/widgets/data_display/indicator.dart`.

`BasicTheme` registers `IndicatorDelegate: BasicIndicatorDelegate()`.

Public constructors and props: [Indicator](/widgets/data-display/Indicator/).
