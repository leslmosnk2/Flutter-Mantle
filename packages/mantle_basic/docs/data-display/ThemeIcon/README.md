# ThemeIcon

Baseline `ThemeIconDelegate` implementation (`BasicThemeIconDelegate`).

Baseline `ThemeIconDelegate`: colored icon holder.

Source: `lib/src/widgets/data_display/theme_icon.dart`.

`BasicTheme` registers `ThemeIconDelegate: BasicThemeIconDelegate()`.

Public constructors and props: [ThemeIcon](/widgets/data-display/ThemeIcon/).
