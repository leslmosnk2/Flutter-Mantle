# MImage

Baseline `MImageDelegate` implementation (`BasicMImageDelegate`).

Baseline `MImageDelegate`: clipped image with fallback.

Source: `lib/src/widgets/data_display/image.dart`.

`BasicTheme` registers `MImageDelegate: BasicMImageDelegate()`.

Public constructors and props: [MImage](/widgets/data-display/MImage/).
