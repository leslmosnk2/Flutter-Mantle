# InputWrapper

Baseline `InputWrapperDelegate` implementation (`BasicInputWrapperDelegate`).

Baseline `InputWrapperDelegate`: label, description, input, error.

Source: `lib/src/widgets/inputs/input.dart`.

`BasicTheme` registers `InputWrapperDelegate: BasicInputWrapperDelegate()`.

Public constructors and props: [InputWrapper](/widgets/inputs/InputWrapper/).
