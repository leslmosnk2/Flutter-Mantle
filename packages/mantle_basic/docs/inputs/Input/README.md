# Input

Baseline `InputDelegate` implementation (`BasicInputDelegate`).

Baseline `InputDelegate`: chrome around an editable field.

Source: `lib/src/widgets/inputs/input.dart`.

`BasicTheme` registers `InputDelegate: BasicInputDelegate()`.

Public constructors and props: [Input](/widgets/inputs/Input/).
