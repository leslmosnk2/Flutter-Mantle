# Chip

Baseline `ChipDelegate` implementation (`BasicChipDelegate`).

Baseline `ChipDelegate`.

Source: `lib/src/widgets/inputs/chip.dart`.

`BasicTheme` registers `ChipDelegate: BasicChipDelegate()`.

Public constructors and props: [Chip](/widgets/inputs/Chip/).
