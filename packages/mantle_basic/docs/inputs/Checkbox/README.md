# Checkbox

Baseline `CheckboxDelegate` implementation (`BasicCheckboxDelegate`).

Baseline `CheckboxDelegate`.

Source: `lib/src/widgets/inputs/checkbox.dart`.

`BasicTheme` registers `CheckboxDelegate: BasicCheckboxDelegate()`.

Public constructors and props: [Checkbox](/widgets/inputs/Checkbox/).
