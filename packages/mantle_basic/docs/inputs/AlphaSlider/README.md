# AlphaSlider

Baseline `AlphaSliderDelegate` implementation (`BasicAlphaSliderDelegate`).

Baseline `AlphaSliderDelegate`.

Source: `lib/src/widgets/inputs/color_picker.dart`.

`BasicTheme` registers `AlphaSliderDelegate: BasicAlphaSliderDelegate()`.

Public constructors and props: [AlphaSlider](/widgets/inputs/AlphaSlider/).
