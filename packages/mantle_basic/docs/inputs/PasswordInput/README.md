# PasswordInput

Baseline `PasswordInputDelegate` implementation (`BasicPasswordInputDelegate`).

Baseline `PasswordInputDelegate`.

Source: `lib/src/widgets/inputs/password_input.dart`.

`BasicTheme` registers `PasswordInputDelegate: BasicPasswordInputDelegate()`.

Public constructors and props: [PasswordInput](/widgets/inputs/PasswordInput/).
