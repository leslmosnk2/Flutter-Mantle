# ColorPicker

Baseline `ColorPickerDelegate` implementation (`BasicColorPickerDelegate`).

Baseline `ColorPickerDelegate`.

Source: `lib/src/widgets/inputs/color_picker.dart`.

`BasicTheme` registers `ColorPickerDelegate: BasicColorPickerDelegate()`.

Public constructors and props: [ColorPicker](/widgets/inputs/ColorPicker/).
