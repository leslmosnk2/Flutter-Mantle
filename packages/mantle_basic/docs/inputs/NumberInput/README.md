# NumberInput

Baseline `NumberInputDelegate` implementation (`BasicNumberInputDelegate`).

Baseline `NumberInputDelegate`.

Source: `lib/src/widgets/inputs/number_input.dart`.

`BasicTheme` registers `NumberInputDelegate: BasicNumberInputDelegate()`.

Public constructors and props: [NumberInput](/widgets/inputs/NumberInput/).
