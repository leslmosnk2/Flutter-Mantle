# Slider

Baseline `SliderDelegate` implementation (`BasicSliderDelegate`).

Baseline `SliderDelegate`.

Source: `lib/src/widgets/inputs/slider.dart`.

`BasicTheme` registers `SliderDelegate: BasicSliderDelegate()`.

Public constructors and props: [Slider](/widgets/inputs/Slider/).
