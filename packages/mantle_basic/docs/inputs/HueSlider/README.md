# HueSlider

Baseline `HueSliderDelegate` implementation (`BasicHueSliderDelegate`).

Baseline `HueSliderDelegate`.

Source: `lib/src/widgets/inputs/color_picker.dart`.

`BasicTheme` registers `HueSliderDelegate: BasicHueSliderDelegate()`.

Public constructors and props: [HueSlider](/widgets/inputs/HueSlider/).
