# ChipGroup

Baseline `ChipGroupDelegate` implementation (`BasicChipGroupDelegate`).

Baseline `ChipGroupDelegate`.

Source: `lib/src/widgets/inputs/chip.dart`.

`BasicTheme` registers `ChipGroupDelegate: BasicChipGroupDelegate()`.

Public constructors and props: [ChipGroup](/widgets/inputs/ChipGroup/).
