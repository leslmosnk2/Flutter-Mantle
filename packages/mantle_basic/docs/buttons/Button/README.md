# Button

Baseline `ButtonDelegate` implementation (`BasicButtonDelegate`).

Mantine-like `ButtonDelegate` for the baseline theme.

Source: `lib/src/widgets/buttons/button.dart`.

`BasicTheme` registers `ButtonDelegate: BasicButtonDelegate()`.

Public constructors and props: [Button](/widgets/buttons/Button/).
