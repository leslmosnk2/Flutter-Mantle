# ActionIcon

Baseline `ActionIconDelegate` implementation (`BasicActionIconDelegate`).

Mantine-like `ActionIconDelegate` for the baseline theme.

Source: `lib/src/widgets/buttons/action_icon.dart`.

`BasicTheme` registers `ActionIconDelegate: BasicActionIconDelegate()`.

Public constructors and props: [ActionIcon](/widgets/buttons/ActionIcon/).
