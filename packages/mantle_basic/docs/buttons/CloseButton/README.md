# CloseButton

Baseline `CloseButtonDelegate` implementation (`BasicCloseButtonDelegate`).

Mantine-like `CloseButtonDelegate`: square control with an X icon.

Source: `lib/src/widgets/buttons/close_button.dart`.

`BasicTheme` registers `CloseButtonDelegate: BasicCloseButtonDelegate()`.

Public constructors and props: [CloseButton](/widgets/buttons/CloseButton/).
