# ActionIconGroup

Baseline `ActionIconGroupDelegate` implementation (`BasicActionIconGroupDelegate`).

Mantine-like `ActionIconGroupDelegate`: Mantine `ActionIcon.Group`. Adjacent border-radius collapsing is a v1 gap; children are laid out with no gap.

Source: `lib/src/widgets/buttons/action_icon_group.dart`.

`BasicTheme` registers `ActionIconGroupDelegate: BasicActionIconGroupDelegate()`.

Public constructors and props: [ActionIconGroup](/widgets/buttons/ActionIconGroup/).
