# MTitle

Baseline `MTitleDelegate` implementation (`BasicMTitleDelegate`).

Baseline `MTitleDelegate`: heading levels 1–6.

Source: `lib/src/widgets/typography/title.dart`.

`BasicTheme` registers `MTitleDelegate: BasicMTitleDelegate()`.

Public constructors and props: [MTitle](/widgets/typography/MTitle/).
