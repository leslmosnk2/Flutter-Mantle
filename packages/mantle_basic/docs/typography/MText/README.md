# MText

Baseline `MTextDelegate` implementation (`BasicMTextDelegate`).

Baseline `MTextDelegate`: size, weight, color, truncation.

Source: `lib/src/widgets/typography/text.dart`.

`BasicTheme` registers `MTextDelegate: BasicMTextDelegate()`.

Public constructors and props: [MText](/widgets/typography/MText/).
