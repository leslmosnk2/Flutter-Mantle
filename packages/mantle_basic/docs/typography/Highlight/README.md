# Highlight

Baseline `HighlightDelegate` implementation (`BasicHighlightDelegate`).

Baseline `HighlightDelegate`: `Mark` matching substrings in `MText`.

Source: `lib/src/widgets/typography/highlight.dart`.

`BasicTheme` registers `HighlightDelegate: BasicHighlightDelegate()`.

Public constructors and props: [Highlight](/widgets/typography/Highlight/).
