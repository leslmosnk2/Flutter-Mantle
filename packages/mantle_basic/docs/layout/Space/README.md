# Space

Baseline `SpaceDelegate` implementation (`BasicSpaceDelegate`).

Baseline `SpaceDelegate`: a `SizedBox` from pixels or spacing tokens.

Source: `lib/src/widgets/layout/space.dart`.

`BasicTheme` registers `SpaceDelegate: BasicSpaceDelegate()`.

Public constructors and props: [Space](/widgets/layout/Space/).
