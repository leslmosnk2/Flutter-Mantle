# Splitter

Baseline `SplitterDelegate` implementation (`BasicSplitterDelegate`).

Baseline `SplitterDelegate`: resizable panes with drag handles.

Source: `lib/src/widgets/layout/splitter.dart`.

`BasicTheme` registers `SplitterDelegate: BasicSplitterDelegate()`.

Public constructors and props: [Splitter](/widgets/layout/Splitter/).
