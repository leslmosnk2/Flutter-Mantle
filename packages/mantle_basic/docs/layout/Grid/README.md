# Grid

Baseline `GridDelegate` implementation (`BasicGridDelegate`).

Baseline `GridDelegate`: packs `GridCol` children into rows.

Source: `lib/src/widgets/layout/grid.dart`.

`BasicTheme` registers `GridDelegate: BasicGridDelegate()`.

Public constructors and props: [Grid](/widgets/layout/Grid/).
