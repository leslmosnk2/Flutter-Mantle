# GridCol

Baseline `GridColDelegate` implementation (`BasicGridColDelegate`).

Baseline `GridColDelegate`: the cell body (sizing is applied by `Grid`).

Source: `lib/src/widgets/layout/grid.dart`.

`BasicTheme` registers `GridColDelegate: BasicGridColDelegate()`.

Public constructors and props: [GridCol](/widgets/layout/GridCol/).
