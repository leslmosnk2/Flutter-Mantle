# MCenter

Baseline `MCenterDelegate` implementation (`BasicMCenterDelegate`).

Baseline `MCenterDelegate`: aligns `child` to the center of the parent.

Source: `lib/src/widgets/layout/center.dart`.

`BasicTheme` registers `MCenterDelegate: BasicMCenterDelegate()`.

Public constructors and props: [MCenter](/widgets/layout/MCenter/).
